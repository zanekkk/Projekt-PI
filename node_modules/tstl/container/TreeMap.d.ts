/** @module std */
import { UniqueMap } from "../base/container/UniqueMap";
import { ITreeMap } from "../base/container/ITreeMap";
import { MapIterator, MapReverseIterator } from "../base/iterator/MapIterator";
import { IForwardIterator } from "../iterator/IForwardIterator";
import { IPair } from "../utility/IPair";
import { Pair } from "../utility/Pair";
/**
 * Unique-key Map based on Tree.
 *
 * @author Jeongho Nam <http://samchon.org>
 */
export declare class TreeMap<Key, T> extends UniqueMap<Key, T, TreeMap<Key, T>> implements ITreeMap<Key, T, true, TreeMap<Key, T>> {
    /**
     * @hidden
     */
    private tree_;
    /**
     * Default Constructor.
     *
     * @param comp A binary function predicates *x* element would be placed before *y*. When returns `true`, then *x* precedes *y*. Note that, because *equality* is predicated by `!comp(x, y) && !comp(y, x)`, the function must not cover the *equality* like `<=` or `>=`. It must exclude the *equality* like `<` or `>`. Default is {@link less}.
     */
    constructor(comp?: (x: Key, y: Key) => boolean);
    /**
     * Initializer Constructor.
     *
     * @param items Items to assign.
     * @param comp A binary function predicates *x* element would be placed before *y*. When returns `true`, then *x* precedes *y*. Note that, because *equality* is predicated by `!comp(x, y) && !comp(y, x)`, the function must not cover the *equality* like `<=` or `>=`. It must exclude the *equality* like `<` or `>`. Default is {@link less}.
     */
    constructor(items: IPair<Key, T>[], comp?: (x: Key, y: Key) => boolean);
    /**
     * Copy Constructor.
     *
     * @param obj Object to copy.
     */
    constructor(obj: TreeMap<Key, T>);
    /**
     * Range Constructor.
     *
     * @param first Input iterator of the first position.
     * @param last Input iterator of the last position.
     * @param comp A binary function predicates *x* element would be placed before *y*. When returns `true`, then *x* precedes *y*. Note that, because *equality* is predicated by `!comp(x, y) && !comp(y, x)`, the function must not cover the *equality* like `<=` or `>=`. It must exclude the *equality* like `<` or `>`. Default is {@link less}.
     */
    constructor(first: Readonly<IForwardIterator<IPair<Key, T>>>, last: Readonly<IForwardIterator<IPair<Key, T>>>, comp?: (x: Key, y: Key) => boolean);
    /**
     * @inheritDoc
     */
    clear(): void;
    /**
     * @inheritDoc
     */
    swap(obj: TreeMap<Key, T>): void;
    /**
     * @inheritDoc
     */
    find(key: Key): TreeMap.Iterator<Key, T>;
    /**
     * @inheritDoc
     */
    key_comp(): (x: Key, y: Key) => boolean;
    /**
     * @inheritDoc
     */
    value_comp(): (x: IPair<Key, T>, y: IPair<Key, T>) => boolean;
    /**
     * @inheritDoc
     */
    lower_bound(key: Key): TreeMap.Iterator<Key, T>;
    /**
     * @inheritDoc
     */
    upper_bound(key: Key): TreeMap.Iterator<Key, T>;
    /**
     * @inheritDoc
     */
    equal_range(key: Key): Pair<TreeMap.Iterator<Key, T>, TreeMap.Iterator<Key, T>>;
    /**
     * @inheritDoc
     */
    emplace(key: Key, val: T): Pair<TreeMap.Iterator<Key, T>, boolean>;
    /**
     * @inheritDoc
     */
    emplace_hint(hint: TreeMap.Iterator<Key, T>, key: Key, val: T): TreeMap.Iterator<Key, T>;
    /**
     * @hidden
     */
    protected _Insert_by_range<InputIterator extends Readonly<IForwardIterator<IPair<Key, T>, InputIterator>>>(first: InputIterator, last: InputIterator): void;
    /**
     * @hidden
     */
    protected _Handle_insert(first: TreeMap.Iterator<Key, T>, last: TreeMap.Iterator<Key, T>): void;
    /**
     * @hidden
     */
    protected _Handle_erase(first: TreeMap.Iterator<Key, T>, last: TreeMap.Iterator<Key, T>): void;
}
export declare namespace TreeMap {
    type Iterator<Key, T> = MapIterator<Key, T, true, TreeMap<Key, T>>;
    type ReverseIterator<Key, T> = MapReverseIterator<Key, T, true, TreeMap<Key, T>>;
    const Iterator: typeof MapIterator;
    const ReverseIterator: typeof MapReverseIterator;
    type iterator<Key, T> = Iterator<Key, T>;
    type reverse_iterator<Key, T> = ReverseIterator<Key, T>;
    const iterator: typeof MapIterator;
    const reverse_iterator: typeof MapReverseIterator;
}
export import map = TreeMap;
//# sourceMappingURL=TreeMap.d.ts.map