/** @module std */
import { ArrayContainer } from "../base/container/ArrayContainer";
import { ArrayIterator, ArrayReverseIterator } from "../base/iterator/ArrayIterator";
import { IForwardIterator } from "../iterator/IForwardIterator";
/**
 * Vector, an array with variable capacity.
 *
 * @author Jeongho Nam <http://samchon.org>
 */
export declare class Vector<T> extends ArrayContainer<T, Vector<T>> {
    /**
     * @hidden
     */
    private data_;
    /**
     * Default Constructor.
     */
    constructor();
    /**
     * Initializer Constructor.
     *
     * @param items Items to assign.
     */
    constructor(items: Array<T>);
    /**
     * Copy Constructor
     *
     * @param obj Object to copy.
     */
    constructor(obj: Vector<T>);
    /**
     * Fill Constructor.
     *
     * @param size Initial size.
     * @param val Value to fill.
     */
    constructor(n: number, val: T);
    /**
     * Range Constructor.
     *
     * @param first Input iterator of the first position.
     * @param last Input iteartor of the last position.
     */
    constructor(first: Readonly<IForwardIterator<T>>, last: Readonly<IForwardIterator<T>>);
    /**
     * @inheritDoc
     */
    assign(n: number, val: T): void;
    /**
     * @inheritDoc
     */
    assign<InputIterator extends Readonly<IForwardIterator<T, InputIterator>>>(begin: InputIterator, end: InputIterator): void;
    /**
     * @inheritDoc
     */
    clear(): void;
    /**
     * @inheritDoc
     */
    resize(n: number): void;
    /**
     * @inheritDoc
     */
    size(): number;
    /**
     * @inheritDoc
     */
    at(index: number): T;
    /**
     * @inheritDoc
     */
    set(index: number, val: T): void;
    /**
     * Access data.
     *
     * @return An array capsuled by this {@link Vector}.
     */
    data(): Array<T>;
    /**
     * @inheritDoc
     */
    [Symbol.iterator](): IterableIterator<T>;
    /**
     * @inheritDoc
     */
    push(...items: T[]): number;
    /**
     * @inheritDoc
     */
    push_back(val: T): void;
    /**
     * @hidden
     */
    protected _Insert_by_range<InputIterator extends Readonly<IForwardIterator<T, InputIterator>>>(position: Vector.Iterator<T>, first: InputIterator, last: InputIterator): Vector.Iterator<T>;
    /**
     * @inheritDoc
     */
    pop_back(): void;
    /**
     * @hidden
     */
    protected _Erase_by_range(first: Vector.Iterator<T>, last: Vector.Iterator<T>): Vector.Iterator<T>;
    /**
     * @hidden
     */
    equals(obj: Vector<T>): boolean;
    /**
     * @inheritDoc
     */
    swap(obj: Vector<T>): void;
    /**
     * @inheritDoc
     */
    toJSON(): Array<T>;
}
export declare namespace Vector {
    type Iterator<T> = ArrayIterator<T, Vector<T>>;
    type ReverseIterator<T> = ArrayReverseIterator<T, Vector<T>>;
    const Iterator: typeof ArrayIterator;
    const ReverseIterator: typeof ArrayReverseIterator;
    type iterator<T> = Iterator<T>;
    type reverse_iterator<T> = ReverseIterator<T>;
    const iterator: typeof ArrayIterator;
    const reverse_iterator: typeof ArrayReverseIterator;
}
export import vector = Vector;
//# sourceMappingURL=Vector.d.ts.map