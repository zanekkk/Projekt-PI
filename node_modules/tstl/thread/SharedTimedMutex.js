"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : new P(function (resolve) { resolve(result.value); }).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g;
    return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (_) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
var __values = (this && this.__values) || function (o) {
    var m = typeof Symbol === "function" && o[Symbol.iterator], i = 0;
    if (m) return m.call(o);
    return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
};
Object.defineProperty(exports, "__esModule", { value: true });
var List_1 = require("../container/List");
var RuntimeError_1 = require("../exception/RuntimeError");
var global_1 = require("../thread/global");
/**
 * Shared timed mutex.
 *
 * @author Jeongho Nam <http://samchon.org>
 */
var SharedTimedMutex = /** @class */ (function () {
    /* ---------------------------------------------------------
        CONSTRUCTORS
    --------------------------------------------------------- */
    /**
     * Default Constructor.
     */
    function SharedTimedMutex() {
        this.queue_ = new List_1.List();
        this.writing_ = 0;
        this.reading_ = 0;
    }
    /**
     * @hidden
     */
    SharedTimedMutex.prototype._Current_access_type = function () {
        return this.queue_.empty()
            ? null
            : this.queue_.front().accessType;
    };
    /**
     * @hidden
     */
    SharedTimedMutex.prototype._Release = function () {
        var e_1, _a;
        // STEP TO THE NEXT LOCKS
        var last = null;
        try {
            for (var _b = __values(this.queue_), _c = _b.next(); !_c.done; _c = _b.next()) {
                var resolver = _c.value;
                // DIFFERENT ACCESS TYPE COMES?
                if (last !== null && last.accessType !== resolver.accessType)
                    break;
                last = resolver;
                // NOT RESOLVED YET
                if (resolver.handler !== null) {
                    // CALL RESOLVER
                    if (resolver.lockType === 0 /* HOLD */)
                        resolver.handler();
                    else
                        resolver.handler(true);
                    // CLEAR RESOLVER
                    resolver.handler = null;
                }
                // STOP AFTER WRITE LOCK
                if (resolver.accessType === 0 /* WRITE */)
                    break;
            }
        }
        catch (e_1_1) { e_1 = { error: e_1_1 }; }
        finally {
            try {
                if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
            }
            finally { if (e_1) throw e_1.error; }
        }
    };
    /* =========================================================
        LOCK & UNLOCK
            - WRITE LOCK
            - READ LOCK
    ============================================================
        WRITE LOCK
    --------------------------------------------------------- */
    /**
     * @inheritDoc
     */
    SharedTimedMutex.prototype.lock = function () {
        return __awaiter(this, void 0, void 0, function () {
            var _this = this;
            return __generator(this, function (_a) {
                return [2 /*return*/, new Promise(function (resolve) {
                        var resolver = {
                            handler: (_this.writing_++ === 0 && _this.reading_ === 0)
                                ? null
                                : resolve,
                            accessType: 0 /* WRITE */,
                            lockType: 0 /* HOLD */
                        };
                        _this.queue_.push_back(resolver);
                        if (resolver.handler === null)
                            resolve();
                    })];
            });
        });
    };
    /**
     * @inheritDoc
     */
    SharedTimedMutex.prototype.try_lock = function () {
        return __awaiter(this, void 0, void 0, function () {
            return __generator(this, function (_a) {
                if (this.writing_ !== 0 || this.reading_ !== 0)
                    return [2 /*return*/, false];
                ++this.writing_;
                this.queue_.push({
                    handler: null,
                    accessType: 0 /* WRITE */,
                    lockType: 1 /* KNOCK */
                });
                return [2 /*return*/, true];
            });
        });
    };
    /**
     * @inheritDoc
     */
    SharedTimedMutex.prototype.try_lock_for = function (ms) {
        var _this = this;
        return new Promise(function (resolve) {
            var resolver = {
                handler: (_this.writing_++ === 0 && _this.reading_ === 0)
                    ? null
                    : resolve,
                accessType: 0 /* WRITE */,
                lockType: 1 /* KNOCK */
            };
            _this.queue_.push_back(resolver);
            if (resolver.handler === null)
                resolve(true);
            else {
                // AUTOMATIC UNLOCK AFTER TIMEOUT
                var it_1 = _this.queue_.end().prev();
                global_1.sleep_for(ms).then(function () {
                    // HAVE UNLOCKED YET
                    if (it_1.value.handler === null)
                        return;
                    // NOT YET, THEN DO UNLOCK
                    --_this.writing_;
                    if (it_1.prev().next().equals(it_1))
                        _this.queue_.erase(it_1); // POP THE LISTENER
                    // RELEASE IF LASTEST RESOLVER
                    var prev = it_1.prev();
                    if (prev.equals(_this.queue_.end()) || prev.value.handler === null)
                        _this._Release();
                    // RETURN FAILURE
                    resolve(false);
                });
            }
        });
    };
    /**
     * @inheritDoc
     */
    SharedTimedMutex.prototype.try_lock_until = function (at) {
        // COMPUTE MILLISECONDS TO WAIT
        var now = new Date();
        var ms = at.getTime() - now.getTime();
        return this.try_lock_for(ms);
    };
    /**
     * @inheritDoc
     */
    SharedTimedMutex.prototype.unlock = function () {
        return __awaiter(this, void 0, void 0, function () {
            return __generator(this, function (_a) {
                if (this._Current_access_type() !== 0 /* WRITE */)
                    throw new RuntimeError_1.RangeError("This mutex is free on the unique lock.");
                --this.writing_;
                this.queue_.pop_front();
                this._Release();
                return [2 /*return*/];
            });
        });
    };
    /* ---------------------------------------------------------
        READ LOCK
    --------------------------------------------------------- */
    /**
     * @inheritDoc
     */
    SharedTimedMutex.prototype.lock_shared = function () {
        return __awaiter(this, void 0, void 0, function () {
            var _this = this;
            return __generator(this, function (_a) {
                return [2 /*return*/, new Promise(function (resolve) {
                        var resolver = {
                            handler: (_this.writing_ === 0)
                                ? null
                                : resolve,
                            accessType: 1 /* READ */,
                            lockType: 0 /* HOLD */
                        };
                        _this.queue_.push_back(resolver);
                        ++_this.reading_;
                        if (resolver.handler === null)
                            resolve();
                    })];
            });
        });
    };
    /**
     * @inheritDoc
     */
    SharedTimedMutex.prototype.try_lock_shared = function () {
        return __awaiter(this, void 0, void 0, function () {
            return __generator(this, function (_a) {
                if (this.writing_ !== 0)
                    return [2 /*return*/, false];
                ++this.reading_;
                this.queue_.push_back({
                    handler: null,
                    accessType: 1 /* READ */,
                    lockType: 1 /* KNOCK */
                });
                return [2 /*return*/, true];
            });
        });
    };
    /**
     * @inheritDoc
     */
    SharedTimedMutex.prototype.try_lock_shared_for = function (ms) {
        var _this = this;
        return new Promise(function (resolve) {
            var resolver = {
                handler: (_this.writing_ === 0)
                    ? null
                    : resolve,
                accessType: 1 /* READ */,
                lockType: 1 /* KNOCK */
            };
            ++_this.reading_;
            _this.queue_.push_back(resolver);
            if (resolver.handler === null)
                resolve(true);
            else {
                // AUTOMATIC UNLOCK AFTER TIMEOUT
                var it_2 = _this.queue_.end().prev();
                global_1.sleep_for(ms).then(function () {
                    if (it_2.value.accessType === null)
                        return;
                    // DO UNLOCK
                    --_this.reading_;
                    if (it_2.prev().next().equals(it_2))
                        _this.queue_.erase(it_2); // POP THE LISTENER
                    // RELEASE IF LASTEST RESOLVER
                    var prev = it_2.prev();
                    if (prev.equals(_this.queue_.end()) || prev.value.handler === null)
                        _this._Release();
                    // RETURN FAILURE
                    resolve(false);
                });
            }
        });
    };
    /**
     * @inheritDoc
     */
    SharedTimedMutex.prototype.try_lock_shared_until = function (at) {
        // COMPUTE MILLISECONDS TO WAIT
        var now = new Date();
        var ms = at.getTime() - now.getTime();
        return this.try_lock_shared_for(ms);
    };
    /**
     * @inheritDoc
     */
    SharedTimedMutex.prototype.unlock_shared = function () {
        return __awaiter(this, void 0, void 0, function () {
            return __generator(this, function (_a) {
                if (this._Current_access_type() !== 1 /* READ */)
                    throw new RuntimeError_1.RangeError("This mutex is free on the shared lock.");
                --this.reading_;
                this.queue_.pop_front();
                this._Release();
                return [2 /*return*/];
            });
        });
    };
    return SharedTimedMutex;
}());
exports.SharedTimedMutex = SharedTimedMutex;
exports.shared_timed_mutex = SharedTimedMutex;
//# sourceMappingURL=SharedTimedMutex.js.map